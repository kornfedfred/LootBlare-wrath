name: Release

on:
  push:
    tags:
      - "[0-9]*"

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: ${{ github.ref }}

      - name: Update Version in TOC and Push
        run: |
          # Configure Git
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          # Replace the version line in the .toc file
          sed -i "s/^## Version: .*/## Version: ${{ github.ref_name }}/g" LootBlare-wrath.toc

          # Check if there are changes to commit
          if git diff --quiet LootBlare-wrath.toc; then
            echo "No changes to TOC file"
          else
            git add LootBlare-wrath.toc
            git commit -m "chore: update version to ${{ github.ref_name }} [skip ci]"
            git push origin HEAD:${{ github.event.repository.default_branch }}
          fi

      - name: Build zip
        run: ./scripts/package.sh "${{ github.ref_name }}"

      - name: Create Versioned GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: LootBlare-wrath.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Update Latest Tag
        run: |
          git tag -f latest
          git push origin latest --force

      - name: Update Latest GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: LootBlare-wrath.zip
          tag_name: latest
          name: Latest Release
          body: "This release always contains the most recent version (${{ github.ref_name }})."
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
